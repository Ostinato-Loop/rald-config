name: Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: deploy-rald-config
  cancel-in-progress: false

jobs:
  deploy:
    name: Deploy to Cloudflare Workers (config.rald.cloud)
    runs-on: ubuntu-latest
    environment: production
    permissions:
      contents: read
      deployments: write
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install dependencies
        run: npm install

      - name: Resolve KV namespace IDs via Cloudflare API
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          node << 'JSEOF'
          const fs = require('fs');
          const h = {
            'Authorization': `Bearer ${process.env.CLOUDFLARE_API_TOKEN}`,
            'Content-Type': 'application/json'
          };
          const base = `https://api.cloudflare.com/client/v4/accounts/${process.env.CLOUDFLARE_ACCOUNT_ID}/storage/kv/namespaces`;

          async function listAll() {
            const r = await fetch(`${base}?per_page=100`, { headers: h });
            const d = await r.json();
            if (!d.success) throw new Error(JSON.stringify(d.errors));
            return d.result;
          }

          async function resolveId(title) {
            const all = await listAll();
            const existing = all.find(n => n.title === title);
            if (existing) { console.log(`  [existing] ${title}`); return existing.id; }
            const r = await fetch(base, {
              method: 'POST', headers: h,
              body: JSON.stringify({ title })
            });
            const d = await r.json();
            if (!d.success) {
              const retry = (await listAll()).find(n => n.title === title);
              if (retry) return retry.id;
              throw new Error(JSON.stringify(d.errors));
            }
            console.log(`  [created]  ${title}`);
            return d.result.id;
          }

          (async () => {
            const RATE_LIMIT_KV   = await resolveId('rald-config-rate-limit');
            const FLAG_CACHE_KV   = await resolveId('rald-config-flag-cache');
            const KILL_SWITCH_KV  = await resolveId('rald-config-kill-switch');
            const COUNTRY_CACHE_KV = await resolveId('rald-config-country-cache');

            let toml = fs.readFileSync('wrangler.toml', 'utf8');
            toml = toml.replace('REPLACE_WITH_RATE_LIMIT_KV_ID',    RATE_LIMIT_KV);
            toml = toml.replace('REPLACE_WITH_FLAG_CACHE_KV_ID',     FLAG_CACHE_KV);
            toml = toml.replace('REPLACE_WITH_KILL_SWITCH_KV_ID',    KILL_SWITCH_KV);
            toml = toml.replace('REPLACE_WITH_COUNTRY_CACHE_KV_ID',  COUNTRY_CACHE_KV);
            fs.writeFileSync('wrangler.toml', toml);
            console.log('wrangler.toml patched with real KV IDs.');
          })().catch(e => { console.error(e); process.exit(1); });
          JSEOF

      - name: Deploy rald-config Worker
        run: npx wrangler deploy
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

      - name: Ensure config.rald.cloud DNS record exists
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          node -e "
          const h={'Authorization':'Bearer '+process.env.CLOUDFLARE_API_TOKEN,'Content-Type':'application/json'};
          (async()=>{
            const z=await fetch('https://api.cloudflare.com/client/v4/zones?name=rald.cloud',{headers:h}).then(r=>r.json());
            const zid=z.result?.[0]?.id; if(!zid){console.log('zone not found');return;}
            const ex=await fetch('https://api.cloudflare.com/client/v4/zones/'+zid+'/dns_records?name=config.rald.cloud',{headers:h}).then(r=>r.json());
            if(ex.result?.length){console.log('config DNS exists');return;}
            const r=await fetch('https://api.cloudflare.com/client/v4/zones/'+zid+'/dns_records',{method:'POST',headers:h,body:JSON.stringify({type:'AAAA',name:'config',content:'100::',proxied:true})}).then(r=>r.json());
            console.log(r.success?'Created config.rald.cloud AAAA 100::':'DNS error:'+JSON.stringify(r.errors));
          })().catch(e=>console.error('[WARN]',e.message));
          "

      - name: Push secrets to Worker
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          RALD_JWT_SECRET: ${{ secrets.RALD_JWT_SECRET }}
          RALD_ADMIN_SECRET: ${{ secrets.RALD_ADMIN_SECRET }}
        run: |
          echo "$SUPABASE_SERVICE_ROLE_KEY" | npx wrangler secret put SUPABASE_SERVICE_ROLE_KEY
          echo "$RALD_JWT_SECRET"           | npx wrangler secret put RALD_JWT_SECRET
          echo "$RALD_ADMIN_SECRET"         | npx wrangler secret put RALD_ADMIN_SECRET
